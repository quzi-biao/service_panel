version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3005:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - dev-panel-network

networks:
  dev-panel-network:
    driver: bridge
